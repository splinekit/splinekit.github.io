.. _api-closed:

Closed [class]
==============

The class :class:`splinekit.interval.Closed` is the concrete class that handles the bounded intervals that are left-closed and right-closed.

.. automodule:: splinekit.interval.closed
   :members:
   :show-inheritance:
   :special-members: __new__, __contains__
   :member-order: bysource
