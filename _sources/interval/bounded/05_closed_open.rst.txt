.. _api-closed_open:

ClosedOpen [class]
==================

The class :class:`splinekit.interval.ClosedOpen` is the concrete class that handles the bounded intervals that are left-closed and right-open.

.. automodule:: splinekit.interval.closed_open
   :members:
   :show-inheritance:
   :special-members: __new__, __contains__
   :member-order: bysource
