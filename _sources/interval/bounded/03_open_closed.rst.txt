.. _api-open_closed:

OpenClosed [class]
==================

The class :class:`splinekit.interval.OpenClosed` is the concrete class that handles the bounded intervals that are left-open and right-closed.

.. automodule:: splinekit.interval.open_closed
   :members:
   :show-inheritance:
   :special-members: __new__, __contains__
   :member-order: bysource
