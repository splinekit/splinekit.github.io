.. _api-above:

Above [class]
=============

The class :class:`splinekit.interval.Above` is the concrete class that handles the left-bounded half-bounded intervals that are left-open.

.. automodule:: splinekit.interval.above
   :members:
   :show-inheritance:
   :special-members: __new__, __contains__
   :member-order: bysource
