.. _api-empty:

Empty [class]
=============

The class :class:`splinekit.interval.Empty` is the concrete class that handles the empty interval.

.. automodule:: splinekit.interval.empty
   :members:
   :show-inheritance:
   :special-members: __new__, __contains__
   :member-order: bysource
